# Claim 2 evaluation

The candidate verdict is `VERIFIED` only if the population certificate,
320-cell exhaustive finite audit, 32 diffuse dispersion identities,
independent raw-witness checker, and squared-distance destructive control all
pass under the unchanged command:

`uv run --frozen --python 3.12 python -m cdot_repro.run`

The cumulative run also reruns every Claim 1 gate.

