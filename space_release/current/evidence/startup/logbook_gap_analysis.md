# Claim-by-claim evaluator-visible gap analysis

The exact judged candidate is
`DineshAI/nPC7M7XLEv@1f2e1bcdc00bd792921b6b010c90fe8120f78405`.
The comparison logbook was inspected at public HEAD
`893c45fe7edb0158c640a49e439da21efd81e34f`. Its conclusions and outputs are
not treated as evidence for this campaign.

| Claim | Judged DineshAI artifact | Sound reference pattern to independently test | Required current work |
| --- | --- | --- | --- |
| 1 | One-line toy convexity summary; no code or proof | Population proof obligations plus executable PSD/Jensen checks and a negated-quadratic control | Reconstruct proof, execute primary and independent checkers, publish raw rows |
| 2 | Toy examples only | Glued-coupling proof obligations, direct conditional-variance identity, squared-distance destructive control | Reconstruct proof and rerun exact finite identities with raw outputs |
| 3 | Wrong scale/protocol and reversed CDOT/FGW direction | Audit `n=N/4`, run `N=2000`, exact Hungarian oracles, independent trials | Full-scale CPU-upgrade rerun with uncertainty and a deliberately mismatched-scale control |
| 4 | Explicitly deferred | Audit all source sessions and the first-100/all-4,950-pair protocol; retain graph-Laplacian sensitivity | Independently fetch and hash primary data; verify the 696-by-170 invariant and, if feasible, rerun Table 3 |
| 5 | Cycle-vs-star proxy with the wrong classifier | Full MUTAG/ENZYMES pair matrices, Gaussian-kernel SVM, nested 10-fold CV, label-permutation control | Rerun the public datasets and exact disclosed grid on CPU-upgrade |
| 6 | Toy bound formula cells | Assumption-complete analytical certificate, separated optimization/statistical terms, executed FW traces | Reconstruct proof obligations and rerun non-circular rate diagnostics |

## Navigation gap

The judged Space exposes only `overview` and `verify`. It has no claim-specific
canonical pages, no executable code, no raw links, no independent checker
output, no controls, no pinned environment, and no discoverable limitations.
The old overview also calls the rejected baseline “10/10,” contradicting the
live judge. The final candidate must put current verification first and label
that page exactly **Historical rejected baseline** without deleting it.

## Adoption boundary

Only method and presentation patterns are adopted. No reference numerical
conclusion, raw output, credential, hidden state, or release claim is imported.
Every adopted computation must execute from this repository's fixed command
and its own pinned environment.

